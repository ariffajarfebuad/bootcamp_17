<nav class="navbar navbar-expand-lg navbar-light bg-white">
  <div class="container">
    <a class="navbar-brand" href="#" style="font-weight:700; letter-spacing:1px;">contact app</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" style="font-weight:700;" aria-current="page" href="/">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" style="font-weight:700;" href="/about">About</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" style="font-weight:700;" href="/contact">Contact</a>
        </li>
      </ul>
    </div>
  </div>
</nav>
<script>
// Efek animasi klik pada navbar
const navLinks = document.querySelectorAll('.nav-anim');
navLinks.forEach(link => {
  link.addEventListener('mousedown', function() {
    // this.style.transform = 'scale(0.93)';
    // this.style.transition = 'transform 0.12s cubic-bezier(.4,2,.6,1)';
  });
  link.addEventListener('mouseup', function() {
    this.style.transform = '';
  });
  link.addEventListener('mouseleave', function() {
    this.style.transform = '';
  });
});
</script>
<style>
.navbar .nav-link {
  font-weight: 800 !important;
  letter-spacing: 0.5px;
  transition: color 0.18s, background 0.18s, border-bottom 0.18s;
  border-radius: 8px 8px 0 0;
  padding: 0.5rem 1.1rem 0.4rem 1.1rem;
  border-bottom: 2.5px solid transparent;
  position: relative;
}
.navbar .nav-link.active, .navbar .nav-link:focus {
  background: rgba(0,113,227,0.10);
  color: #0071e3 !important;
  border-bottom: 2.5px solid #0071e3;
}
.navbar .nav-link:hover {
  background: rgba(0,113,227,0.07);
  color: #0071e3 !important;
  border-bottom: 2.5px solid #0071e3;
}
</style>